# AI修仙模拟器

**给 AI 重度用户的桌面工作台。** 不是修仙游戏，是干活工具：
桌面贴纸实时显示今天每个 AI 会话的状态，长任务完成敲木鱼提醒，定时任务失职报警。

修仙是记账方式：你平时怎么用 AI 就怎么用，系统把每个真实任务折算成灵气，
攒修为、升境界、悟机缘。炼气 → 筑基 → 金丹 → 元婴 → 化神 → 炼虚 → 合体 → 大乘 → 渡劫 → 飞升。

## 需要什么

- Windows 10/11
- Python 3.8+（python.org 下载，安装时勾选 Add to PATH）
- 你在用 **Claude Code** 或 **Codex**（CLI/Desktop）——两个都用经验直接叠加

## 安装

1. 解压到任意目录（路径别再挪，挪了重跑一次安装）
2. Windows 双击 `①双击我安装.bat`，苹果电脑双击 `①双击我安装.command`
3. 弹出的窗口里取个道号，点「开始修行」
4. 完成。你的全部历史使用会立刻结算成等级，用得久的人开局就不是道童

## 怎么玩

主界面是装完自动弹出的**桌面任务贴纸**（✕ 关掉当天不弹，双击 `①双击我安装.bat` 随时打开）：

- **修仙状态栏**：修为等级 + 今日灵气 + 连修天数，点徽章直接出等级卡
- **历练页**：今天每个 AI 会话的实时状态（⏸ 等你确认 / ◐ 运行中 / ✓ 已完成）
- **机缘页**：已悟图鉴 + 未悟剪影；**战报页**：今日小结 + 看面板/出周报/重新结算按钮
- **等级卡**：贴纸右上 ⤴ 一键出卡到桌面，直接发群里比
- **升级弹窗+木鱼声**：Claude Code 用户每次会话结束秒级结算，长任务完成敲一声木鱼弹「历练完成」，境界突破当场弹窗；其他工具每小时自动结算一次
- 八个方向：程序 / 策划 / 影视 / 美术 / 文案 / 研究 / 运营 / 杂学，各自独立升境界
- 嫌弹窗吵：点贴纸右上的「铃」（护法警报不受影响）

## 规则

不公开。做什么涨得快、机缘怎么解锁，自己悟，或者跟其他道友交流。
只说三件事：

1. 灵气只来自真实使用，没有任何行为可以刷
2. 挂机跑的自动任务，灵气稀薄
3. 境界越往后越难，99 级飞升是以年计的事

## 高级玩法：护法（自动任务监督，可选）

如果你也让 AI 跑定时任务（内容引擎、数据采集之类），可以让本系统当监督总管：
照着 `watchdog.py` 开头注释建一个 `watchdog-tasks.json` 时刻表，护法每小时核对
所有任务是否运转，谁没跑弹红色警报「护法示警：阵法停转」。
不建这个文件，此功能自动关闭，不影响其他一切。

## 隐私

所有数据只在你自己电脑上算，不上传任何东西。
等级卡上只有等级、境界、机缘名，不会出现你干了什么任务的任何内容，放心晒。

## 卸载

删掉目录，再删掉计划任务「AI修仙-数据源轮询」（任务计划程序里删除）。
Claude Code 用户另在 `~/.claude/settings.json` 里删掉含 settle.py / notify_mark.py / stickyboot 的三条 hook。

——

反馈直接群里说：哪里不好玩、哪里看不懂、想比什么。
